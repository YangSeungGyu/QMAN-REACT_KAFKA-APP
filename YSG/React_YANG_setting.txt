
=================루트경로 설정 + json을 자동배열로 인식하게
루트 경로 jsconfig.json 생성해서 환경 설정 파일 만들기
[jsconfig.json] 생성
{
  "compilerOptions": {
    "baseUrl": ".",
    "paths": {
      "@/*": ["src/*"] 
    }
  },
  "include": ["src"]
}

[vite.config.js] 수정
import path from 'path'

 resolve: {
    alias: {
      // 현재 경로의 src를 'src'라는 이름으로 매핑
      "src": path.resolve(__dirname, "./src"),
    },
  },

  ==============


  npm install react-router-dom

===========실행 명령어 확인============
package.json
기본
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "lint": "eslint .",
    "preview": "vite preview"
  }
코드ui
 "scripts": {
    "build": "vite build",
    "lint": "eslint",
    "serve": "vite preview",
    "start": "vite"
  },
  
  ** vite쪽이 test,start같은건 run 없이도 실행됨