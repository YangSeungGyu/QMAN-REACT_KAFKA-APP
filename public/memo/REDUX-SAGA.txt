============redux saga 설정===========
stroe
	└rootReducer.js
	└store.js

sagas
	└postSaga.js
	└rootSaga.js

features
	└post
		└postSlice.js
		└postApi.js
	
-----------------------
main.jsx << 수정
import { Provider } from 'react-redux';
import store from './store/store';

<Provider store={store}>
	<App />
</Provider>

-----------------------
TestSaga.jsx 에서 호출

=================셋팅에 따른 설정=========================
01.main.jsx 에서 /store/store를 Provider로 감쌈

02.store.js에서 rootReducer store에 연결하고 rootSaga를 run (미들웨어에연결)

03.rootReducer.js에서는 모든reducer를 셋팅

04.rootSaga.js에 모든 saga 연결

05.각 saga에서는 slice와 api를 연결(slice에서 리듀서 생성)



개념:
Slice : 결과를받을변수,상태값변수들과 + Reducer를 포함한 파일
Api : axios api요청을 함수로 분리(Saga에서 호출함)
Reducer : 각 비동기에 요청,결과에대한 함수.
Saga : Api와 Reducer함수를 요청별로 셋팅하여 Worker로 생성 후 takeLatest에 셋팅하여 Saga로 생성
=================saga사용=========================

features의 postApi.js에 api 호출작성
features의 postSlice.js에 데이터 포맷 및 request,success,fial 작성

const dispatch = useDispatch();
const { data, loading, error } = useSelector((state) => state.post);

import { useDispatch, useSelector } from 'react-redux';
import { fetchPostsRequest } from '@/features/post/postSlice';

const param = {id:'aaa'};

dispatch(fetchPostsRequest(param)); << 사가 호출

data< 이걸로 데이터 활용