========================================================
function Page() {

    // 최초 화면 조회
    useEffect(() => {
        search();
    }, []);

    // 조회
    const search = async () => {
        const data = await api.getUsers();
        setUsers(data);
    };

    // 저장
    const save = async () => {
        await api.save(users);
        alert("저장 완료");
    };

    return (
        <>
            <Button onClick={search}>조회</Button>
            <Button onClick={save}>저장</Button>
        </>
    );
}

========================================================
import {useState,useEffect} from 'react'
import * as menu02Js from '@/js/Menu02Page.js'

function Menu02Page(){
    const [val,setVal] = useState(0);
    const [val2,setVal2] = useState(0);

    useEffect(()=>{
        //초기설정 여러개 해야할때.  useEffect안에서 set함수를 두번이상 호출하면 안되니 감싸야함.
        const init = () => {
            menu02Js.testFn(val,setVal);
            alert(val);

            setVal2(menu02Js.test2Fn(val2));
            alert(val2);
        }
        init();

    },[])



    return(
        <>test22222222</>
    )
}
export default Menu02Page;

------Menu02Page.js-----

export const testFn = (val,setVal) => {
    const addNum = 1;
    setVal(val+addNum);
}

export const test2Fn = (val) => {
    const addNum = 1;
    return val+addNum;
}
